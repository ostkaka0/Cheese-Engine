#ifndef _SERVER
#include "GraphicalContainer.h"

namespace gui
{
	GraphicalContainer::GraphicalContainer(int x, int y, int width, int height)
	{
		m_x = x;
		m_y = y;
		m_width = width;
		m_height = height;

	}
}
#endif